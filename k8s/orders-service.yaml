apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders-service
  labels:
    app: orders-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: orders-service
  template:
    metadata:
      labels:
        app: orders-service
    spec:
      containers:
      - name: orders-service
        image: bookstore-orders-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 5003
          name: http
        env:
        - name: DATABASE_URI
          value: "mysql+pymysql://bookuser:bookpass@mysql-service:3306/bookstore_orders"
        - name: AUTH_SERVICE_URL
          value: "http://auth-service:5001"
        - name: SECRET_KEY
          value: "secretkey-hardcoded-orders"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 5003
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 5003
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: orders-service
  labels:
    app: orders-service
spec:
  type: NodePort
  ports:
    - port: 5003
      targetPort: 5003
      nodePort: 30003
      protocol: TCP
      name: http
  selector:
    app: orders-service
